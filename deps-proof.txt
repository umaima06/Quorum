=== requirements.txt (empty = no declared third-party runtime deps) ===
(file has 0 lines)

=== Every top-level import in quorum.py ===
import argparse
import hashlib
import hmac
import http.server
import json
import os
import secrets
import smtplib
import socketserver
import ssl
import sys
import threading
import time
import webbrowser
from http.cookies import SimpleCookie
from email.message import EmailMessage
from pathlib import Path

=== Each import checked against the Python 3 standard library ===
All modules above (argparse, hashlib, hmac, http.server, json, os, secrets,
smtplib, socketserver, ssl, sys, threading, time, webbrowser,
email.message, http.cookies, pathlib) ship with CPython — see:
https://docs.python.org/3/library/

Note: 'pip list' is intentionally NOT used here — it reports every
package installed in the current environment (including unrelated
system/dev tools), not what this project actually imports. The
import grep above is the accurate, verifiable proof.